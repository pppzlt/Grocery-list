{{!-- list cards that populate based on how many items are in db --}}
<div class="card mt-3">
    <div class="card-body d-flex justify-content-between shadow">
        <div>
            <a href="/innerlist/{{list_id}}"><h5
                    class="card-title"
                >{{list_name}}</h5></a>
            <h6 class="card-subtitle mb-2 text-muted">{{list_descrip}}</h6>
        </div>
        <div class="d-flex flex-row align-items-center justify-content-center">
            <h4 hidden>{{list_id}}</h4>
                <a href="javascript:;" class="card-link"></a>
            <div class="material-symbols-outlined px-3" id="delete" style="cursor: pointer">
                delete
            </div>
        </div>
    </div>
</div>